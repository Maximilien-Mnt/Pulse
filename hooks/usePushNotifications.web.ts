// ---------------------------------------------------------------------------
// PULSE — usePushNotifications (web stub)
//
// Expo push tokens are not supported on web: this is a no-op so Metro's web
// export never bundles `expo-notifications`. The native implementation lives
// in `usePushNotifications.ts` and is picked up automatically by Metro's
// platform resolution.
// ---------------------------------------------------------------------------

export function usePushNotifications() {
  return undefined;
}
